# TeamMemberRoleRef Component Generation Specification

## Overview
Generate components for `entityName=TeamMemberRoleRef` based on the reference example `copyFromEntity=providertyperef`.

## File Structure
All component files will be contained in: `src/app/components/_crud/teammemberroleref/`

### Files to Generate:
- `teammemberroleref-crud.component.html`
- `teammemberroleref-crud.component.ts`
- `teammemberroleref-group.component.html`
- `teammemberroleref-group.component.ts`
- `teammemberroleref-group.component.scss`
- `teammemberroleref-list.component.ts` (NO template or stylesheet files)

---

## Data Interfaces & Service Methods

### Interfaces by Mode:
- **Details:** `TeamMemberRoleRefGETData` (getMode() === CRUD_MODES.DETAIL)
- **Edit:** `TeamMemberRoleRefPUTData` (getMode() === CRUD_MODES.EDIT)
- **Create:** `TeamMemberRoleRefPOSTData` (getMode() === CRUD_MODES.CREATE)

### Service Methods:
- **Get:** `HcclService.getTeamMemberRoleRefById(id)`
- **Create:** `HcclService.createTeamMemberRoleRef(body)`
- **Update:** `HcclService.updateTeamMemberRoleRefById(id, body)`
- **Search:** `HcclService.findTeamMemberRoleRefs(criteria)`

---

## Field Display Guidelines

### String Values
- **Textarea:** Use if MaxLength > 100 OR ends with "text", "description", "notes", "comments"
  - Component: `<app-std-mdb-textarea>`
- **Text Input:** Use for all other strings
  - Component: `<app-std-mdb-text>`
- **Parameters:** Include `required`, `maxLength` from swagger spec

### Date Values
- **Display:** `<app-dategetdata>`
- **Edit:** `<app-std-mdb-datepicker>`

### Boolean Values
- **Component:** `<app-std-mdb-boolean>` (default checkbox)

### Available Field
- **Component:** `<app-available-selector>`

### Hidden Fields (Create/Update)
- `id`, `dateLastUpdated`, `dateCreated`, `lastUpdatedBy`, `createdBy`

---

## Component Specifications

## 1. CRUD Component

### Purpose
Display, edit, and add `TeamMemberRoleRef` data.

### Files
- `teammemberroleref-crud.component.html` (use `./templates/template-crud.component.html`)
- `teammemberroleref-crud.component.ts` (use `./templates/template-crud.component.ts`)
- **Stylesheet:** `'../../_global/abstract-crud/abstract-crud.component.scss'`

### Required Imports
```typescript
import { CommonModule, FormsModule } from '@angular/forms';
import { AbstractCrudComponent } from '@app/components/_global/abstract-crud/abstract-crud.component';
import { EntityWrapper } from '@app/models/crud-entity-wrapper';
import { HcclService } from '@app/restsvc/hccl.service';
import { CRUD_MODES } from '@app/core/constants';
import { SimpleMessagesSectionComponent } from '@app/components/_global/simple-messages-section/simple-messages-section.component';
import { MenuControlDataListComponent } from '@app/components/_global/menu-control-data-list/menu-control-data-list.component';
import { AvailableSelectorComponent } from '@app/components/_global/available-selector/available-selector.component';
import { DategetdataDisplayComponent } from '@app/components/_global/dategetdata-display/dategetdata-display.component';
```

### Component Structure
- **Extend:** `AbstractCrudComponent<TeamMemberRoleRefCrudWrapper>`
- **Implement:** `OnInit, OnChanges`
- **Wrapper Class:** Extends `EntityWrapper<TeamMemberRoleRefGETData>`
- **Constructor:** Only call `super()` - no additional services needed

### Required Abstract Methods
- `loadEntityByIdCall()`
- `createEntityDataCall()`
- `updateEntityDataCall()`
- `deleteEntityData()`
- `newEmptyWrapper()`

### Menu Implementation
```typescript
// In CrudComponent
protected override async prepareMenus(entity: TeamMemberRoleRefCrudWrapper): Promise<void> {
    //const criteria = this.getTeamMemberRoleRefFkMenuCriteria();
    //this.teamMemberRoleRefMenu = await entity.getFkMenu(teamMemberRoleRef, this.id);
    return Promise.resolve();
}

// In CrudWrapper
public override async getFkMenu(menuHint?: string, data?: any): Promise<MenuControlDataList> {
    const teamMemberRoleRefs = await this.getTeamMemberRoleRefs();
    return this.getMenuControlDataList("teamMemberRoleRefs", getEntityType() + " Menu", teamMemberRoleRefs, data);
}
```

### Template Sections

#### Details Section
- **Based on:** `TeamMemberRoleRefGETData`
- **Method:** `this.hcclService.getTeamMemberRoleRefById(id)`
- **Fields:** All attributes, `id` first, audit fields last

#### Section Mode
- **Based on:** `TeamMemberRoleRefGETData`
- **Method:** `this.hcclService.getTeamMemberRoleRefById(id)`
- **Fields:** All attributes except hidden fields (id, audit fields)

#### Edit Section
- **Based on:** `TeamMemberRoleRefPUTData`
- **Method:** `this.hcclService.updateTeamMemberRoleRefById(id, body)`
- **Fields:** All editable attributes

#### Create Section
- **Based on:** `TeamMemberRoleRefPOSTData`
- **Method:** `this.hcclService.createTeamMemberRoleRef(body)`
- **Fields:** All addable attributes

### TeamMemberRoleRef Attributes
Based on the service interfaces, the following attributes should be included:

#### Details/Section Mode Attributes:
- `id` (first, hidden in section mode)
- `name` (string)
- `businessCode` (string)
- `description` (string)
- `available` (number - use available-selector)
- `dateCreated` (date - use dategetdata-display)
- `createdByInfo` (reference)
- `dateLastUpdated` (date - use dategetdata-display)
- `lastUpdatedByInfo` (reference)

#### Edit/Create Mode Attributes:
- `name` (string - required)
- `businessCode` (string - required)
- `description` (string - required)
- `available` (number - use available-selector)

---

## 2. Group Component

### Purpose
Router target to group all tabs for `TeamMemberRoleRef` (details, list).

### Files
- `teammemberroleref-group.component.html` (use `./templates/template-group.component.html`)
- `teammemberroleref-group.component.ts`
- `teammemberroleref-group.component.scss` → `'../../_global/abstract-entity-group/abstract-entity-group.component.scss'`

### Required Imports
```typescript
import { CommonModule } from '@angular/common';
import { ActivatedRoute } from '@angular/router';
import { AbstractEntityGroupComponent } from '@app/components/_global/abstract-entity-group/abstract-entity-group.component';
import { SimpleTab, SimpleTabsetComponent } from '@app/components/_global/simple-tabset/simple-tabset.component';
import { HcclService } from '@app/restsvc/hccl.service';
```

### Component Structure
- **Extend:** `AbstractEntityGroupComponent<TeamMemberRoleRefCrudWrapper>`
- **Implement:** `OnInit`
- **Input:** `@Input() id!: string`
- **Constructor:** Only inject `ActivatedRoute` - do NOT inject `Router` or `HcclService`

### Required Abstract Methods
- `loadEntityById()`
- `setupTabs()`

### Template Structure
- Copy from `providertyperef-group.component.html`
- Replace component references with generated component names
- Include conditional divs for details and list tabs
- Use `*ngIf` for tab content display

---

## 3. List Component

### Purpose
Router target to search and list `TeamMemberRoleRefGETData`.

### Files
- `teammemberroleref-list.component.ts` ONLY (NO template or stylesheet files)

### Required Imports
```typescript
import { CommonModule } from '@angular/common';
import { HcclService } from '@app/restsvc/hccl.service';
import { AbstractListComponent } from '@app/components/_global/abstract-list/abstract-list.component';
import { Observable } from 'rxjs';
```

### Component Structure
- **Extend:** `AbstractListComponent<TeamMemberRoleRefGETData, TeamMemberRoleRefCriteria, TeamMemberRoleRefGETDataSearchResults>`
- **DO NOT** implement `OnInit, AfterViewInit` - use abstract base class
- **Constructor:** Only call `super()` - no additional services needed
- **Copy ALL methods** from `providertyperef-list.component.ts`

### Template and Styles
- **Template:** `'../../_global/abstract-list/abstract-list.component.html'`
- **Stylesheet:** `'../../_global/abstract-list/abstract-list.component.scss'`
- **DO NOT** create custom template or stylesheet files

### Required Abstract Methods
- `getGridColumns()`: Return grid column configuration
- `createCriteria()`: Create search criteria with paging
- `findEntities()`: Call `this.hcclService.findTeamMemberRoleRefs(criteria)`
- `hasSearchResults()`: Check if response has search results
- `getSearchResults()`: Extract search results from response
- `formatEntityData()`: Format entity data for display
- `onRowClick()`: Handle row click navigation
- `onAdd()`: Handle add button click

### Grid Configuration
- Include all `TeamMemberRoleRef` fields in grid columns
- **Comment out audit fields:** id, createdByInfo, dateCreated, lastUpdatedByInfo
- Add proper filtering and alignment for each column
- Use plural route names for navigation

---

## Route Naming Convention

### Pluralization Rules
- **Ends in "y":** Add "ies" (e.g., "entry" → "entries")
- **Ends in "f":** Change to "ves" (e.g., "leaf" → "leaves")
- **Default:** Add "s" (e.g., "catalog" → "catalogs")

### Examples
- entityName = "TeamMemberRoleRef" → route = "teamMemberRoleRefs"

### Route Structure
- **CRITICAL:** Never include package prefixes in routes
- Use: `/ecoadmin-dashboard/[pluralEntityName]` (not `/ecoadmin/[pluralEntityName]`)

---

## Module Integration

### Routing Configuration
Add to appropriate module (e.g., `dash-ecoadmin.module.ts`):
```typescript
{ path: 'teamMemberRoleRefs', component: TeamMemberRoleRefListComponent },
{ path: 'teamMemberRoleRefs/create', component: TeamMemberRoleRefGroupComponent },
{ path: 'teamMemberRoleRefs/:id/:tabId', component: TeamMemberRoleRefGroupComponent },
{ path: 'teamMemberRoleRefs/:id', redirectTo: 'teamMemberRoleRefs/:id/details', pathMatch: 'full' }
```

### Module Imports
```typescript
imports: [
  // ... existing imports
  TeamMemberRoleRefListComponent,
  TeamMemberRoleRefGroupComponent
]
```

---

## Menu Integration

### Menu Constants
Add to `src/app/shell/services/menu.service.ts`:
```typescript
EA_TEAMMEMBERROLEREF_LIST: {
  level: 1,
  label: 'Team Member Role Refs',
  route: '/ecoadmin-dashboard/teamMemberRoleRefs',
  componentPath: 'src/app/components/_crud/teammemberroleref/teammemberroleref-list.component',
  componentName: 'TeamMemberRoleRefListComponent',
  icon: 'fas fa-list'
}
```

### Menu Builder
Update `buildEcoAdminMenu()` method:
```typescript
// Add TeamMemberRoleRef
const teamMemberRoleRef = this.copyMenuItem(MENU_CONSTANTS.EA_TEAMMEMBERROLEREF_LIST);
this.addMenuItem(menu, teamMemberRoleRef);
```

---

## Common Issues & Solutions

### Component Import Errors
- Ensure all required components are imported in `@Component.imports` array
- For date components, ensure `@Input() data: any` is declared
- For menu controls, use: `<app-menu-control-data-list [menuControlDataList]="this.fkMenu" [id]="this.id"></app-menu-control-data-list>`

### Template Binding Errors
- Ensure `@Input()` properties are declared in component classes
- For available selector, ensure proper two-way binding with `[(ngModel)]`
- Check that all form field bindings use correct property names

### Routing Issues
- **CRITICAL:** Ensure route paths match between module configuration and menu constants
- Use consistent route naming: `/ecoadmin-dashboard/[routePath]` for eco-admin
- Include proper redirects for default tab navigation

### Constructor Dependencies
- **CRUD Components:** Only call `super()` - no additional services needed
- **Group Components:** Only inject `ActivatedRoute` in constructor
- **List Components:** Only call `super()` - no additional services needed
- Always call `super()` in constructor for components extending abstract classes

### AbstractListComponent Integration
- **CRITICAL:** List components MUST extend `AbstractListComponent` not implement `OnInit`
- **DO NOT** include `declare const dhx: any;` - handled by abstract base class
- **DO NOT** include manual DHTMLX grid initialization - handled by abstract base class
- **DO NOT** create custom template or stylesheet files - use abstract list template
- Copy ALL abstract method implementations from `providertyperef-list.component.ts`

---

## Idempotent Generation Rules

1. **Template Copying:** Always copy complete structure from reference components or templates
2. **Method Implementation:** Copy ALL methods from reference components, but don't create new ones
3. **Import Requirements:** Include all necessary imports for components and services
4. **Constructor Rules:** Follow strict constructor injection rules for each component type
5. **Routing Consistency:** Ensure menu routes match actual app routing structure using plural entity names
6. **Component Dependencies:** Declare all required `@Input()` properties
7. **Error Handling:** Include proper error handling in data loading methods
8. **List Component Rules:** MUST extend AbstractListComponent, use abstract list template, no custom template files
9. **Route Naming:** Always use plural form of entityName for routes
10. **No Package Prefixes:** Never include package prefixes in routes

---

## Final Checklist

Before generating components, ensure:
- [ ] All required imports are specified
- [ ] Constructor dependencies are correctly defined
- [ ] Routing paths are consistent between module and menu
- [ ] Template copying guidelines are followed
- [ ] Common issues and solutions are addressed
- [ ] Component structure matches reference components
- [ ] All abstract methods are implemented
- [ ] Error handling is included
- [ ] DHTMLX integration is complete (for list components)
- [ ] Menu integration uses correct route paths
- [ ] Templates from `/tooling/prompt/templates/` are used as starting points 