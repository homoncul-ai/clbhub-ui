


- any time we have a property named 'available' - use the 'available-selector' for input
- any time we have a property with a form '[entityName]id' (not the property 'id') this is a 
  foreign key property.  Look for a component of type "app-[entityName]-crud" and use that for
   - 'details' - <app-[entityName]-crud mode='fk' id='[entityName]id' >
   - 'create','edit'  - <app-[entityName]-crud mode='menu' id='[entityName]id' parentEntity='caller-entityName' callback=setProperty(>
   - Remove the "ID" from the label.

   
- We're going to use the clstudent-crud.component.ts and clstudent-crud.component.html 
   as an example to copy.
- We will supply a variable "entityName" - find the HcclServices interfaces for this[entityName]GETData and [entityName]POSTData
     and we will be using the properties from these files.  
  
- Copying CrudWrapper 
   - Create a commented method guts for 'prepareMenu'.  Make a best guess (based on what in "ClstudentCrudWrapper" methods) as to what 
     the implementation should be, but leave it commented out.  Add any existing WrapperComponents that will 
     be required for the FK presentation in the modeName='details' template.
   - Consider the superClass : AbstractCrudComponent - all the control logic to implement the templates 
     is there. 
   - ONLY implement the modeNames that are implemented in clstudent-crud.component.html. 
     
- Copying EntityWrapper 
   - Make sure you copy the static newInstance method with appropriate arguments.
   - Consider the superclass : AbstractEntityWrapper - all the special methods are defined there.
     keep the [entityName]EntityWrapper clean - do not make any new methods. 
   
- All of the control methods for the HTML wrappers can be copied from clstudent-crud.component.html (with appropriate changes)
  .  Do not create any new methods.   

- Copy, modify with  and add these routes to dash-advo.module.ts  
          { path: '[routePath]/:id/:tabId', component: [entityName]GroupComponent },
          { path: '[routePath]/:id', redirectTo: 'schools/:id/details', pathMatch: 'full' },
          { path: '[routePath]', component: [entityName]ListComponent },


In conclusion, be CAREFUL.  Let's make this easy. 

Variables Section

entityName is : WorkRequest 
routePath is  : workrequest
  